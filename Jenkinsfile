// * Groovy - Based off of Java

pipeline {
    agent any // What node you're using, just keep as agent any

    stages {

        stage ('helloWorld') {
            steps {
                sh 'echo helloWorld'
                sh 'pwd'
                sh 'ls'
            }
        }
    }
}